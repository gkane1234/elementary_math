# g6: Greatest common factor

`g6_greatest_common_factor` — continuous difficulty samples for topic-fit / ramp review.

Samples via the **live continuous-D API path** (`QUESTION_TYPES` / `_generate_for_type` / seed / `include_answer_key`).

Open [gallery.html](gallery.html) in a browser for **KaTeX-rendered math** (markdown preview leaves `$...$` as raw LaTeX).

Difficulties: 0, 5, 10, 15, 20, 25 · 2 sample(s) each.

| D | Prompt | Answer | Engine / spend |
|--:|--------|--------|----------------|
| 0 | $\text{Find the GCF of } 6, 9$ | $3$ | — |
| 0 | $\text{Find the GCF of } 6, 9$ | $3$ | — |
| 5 | $\text{Find the GCF of } 15, 10$ | $5$ | — |
| 5 | $\text{Find the GCF of } 21, 7$ | $7$ | — |
| 10 | $\text{Find the GCF of } 44, 22$ | $22$ | — |
| 10 | $\text{Find the GCF of } 14, 42, 28$ | $14$ | — |
| 15 | $\text{Find the GCF of } 45, 90, 135$ | $45$ | — |
| 15 | $\text{Find the GCF of } 45, 90$ | $45$ | — |
| 20 | $\text{Find the GCF of } 72, 108, 36$ | $36$ | — |
| 20 | $\text{Find the GCF of } 108, 36, 72$ | $36$ | — |
| 25 | $\text{Find the GCF of } 72, 144, 216$ | $72$ | — |
| 25 | $\text{Find the GCF of } 72, 144, 216$ | $72$ | — |
