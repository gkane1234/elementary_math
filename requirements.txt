# sympy is used for partial-fraction matrix solving (PFD / rational expressions).
# fastapi is used by backend/main.py on Vercel Services.
# beautifulsoup4 + mathml-to-latex are used by OpenStax HTML mining.
fastapi>=0.115.0
sympy>=1.12
beautifulsoup4>=4.12
mathml-to-latex>=1.0.0