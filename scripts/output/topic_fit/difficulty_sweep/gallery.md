# Difficulty sweep gallery (continuous difficulty only)

Open **[gallery.html](gallery.html)** in a browser for the full KaTeX gallery.

Also see [INDEX.md](INDEX.md) and [index.html](index.html) for TOC / per-course slices.

Regenerate:

```powershell
$env:PYTHONPATH='.'
python scripts/build_difficulty_sweep_gallery.py
```
