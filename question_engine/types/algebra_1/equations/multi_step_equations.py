"""Multi-step equations — primitive generator (catalog + GENERATORS override)."""

# Catalog registration via register_catalog_types() uses GENERATORS["multi_step_equations"]
# (primitive_g6). This module intentionally does not re-bind a legacy framework.
